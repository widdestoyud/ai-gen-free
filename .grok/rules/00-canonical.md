# Canonical rules

This repo’s source of truth for every agent (including you) is root `AGENTS.md`, then `docs/adr/`, then `docs/architecture.md`.

Do not invent architecture. New locked decisions = new ADR. Do not silently edit an accepted ADR.

Steering copies live in `.kiro/steering/`, `.cursor/rules/`, `CLAUDE.md`, `.github/copilot-instructions.md`. If they drift, `AGENTS.md` wins.
