{
  "schema_version": 1,
  "diagram_type": "lifecycle",
  "meta": {
    "title": "Job + points lifecycle",
    "output": "docs/archify/html/job.lifecycle.html",
    "quality_profile": "showcase",
    "views": [
      { "id": "main", "label": "Main path", "focus": ["queued", "running", "succeeded", "cooldown"], "note": "One active job, then a configurable cooldown after success." },
      { "id": "fail", "label": "Failure", "focus": ["running", "failed", "queued"], "note": "Failed jobs release the hold and do not start cooldown." },
      { "id": "ready-again", "label": "Ready again", "focus": ["cooldown", "ready", "queued"], "note": "After cooldown the user may submit another job." }
    ]
  },
  "lanes": [
    { "id": "main", "label": "Job phases" },
    { "id": "waiting", "label": "User wait" },
    { "id": "exceptions", "label": "Recovery" },
    { "id": "terminal", "label": "Media exit" }
  ],
  "states": [
    { "id": "queued", "type": "start", "label": "Queued", "sublabel": "hold posted", "lane": "main", "col": 0, "step": "01", "tag": "mutex" },
    { "id": "running", "type": "active", "label": "Running", "sublabel": "provider work", "lane": "main", "col": 1, "step": "02", "tag": "worker" },
    { "id": "succeeded", "type": "success", "label": "Succeeded", "sublabel": "capture posted", "lane": "main", "col": 2, "step": "03", "tag": "paid" },
    { "id": "cooldown", "type": "waiting", "label": "Cooldown", "sublabel": "admin setting", "lane": "main", "col": 3, "step": "04", "tag": "12h default" },
    { "id": "ready", "type": "active", "label": "Ready", "sublabel": "may submit again", "lane": "main", "col": 4, "step": "05" },
    { "id": "failed", "type": "failure", "label": "Failed", "sublabel": "hold released", "lane": "exceptions", "col": 0, "yOffset": 78, "tag": "no cooldown" }
  ],
  "transitions": [
    { "id": "run-fail", "from": "running", "to": "failed", "variant": "security" },
    { "id": "fail-retry", "from": "failed", "to": "running", "variant": "emphasis", "fromSide": "left", "toSide": "top", "via": [[20, 385], [20, 80], [248, 80]] }
  ],
  "cards": [
    { "dot": "emerald", "title": "Success", "items": ["Capture is idempotent per job id", "Cooldown is stored on the user, not the job"] },
    { "dot": "rose", "title": "Failure", "items": ["Points return via release", "A new generate is allowed immediately"] }
  ]
}
