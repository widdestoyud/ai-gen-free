# SDLC

Slice AIDLC: `aidlc/README.md`.

Conductor: agent `orchestrator`.  
Spesialis: `po`, `sa`, `be`, `fe`, `qa`.

Di Grok:

```
/workflow aidlc-mvp
```

`args.intent` = id unit (contoh `M1-auth`).

Gerbang manusia setelah SA. QA fail-closed.
