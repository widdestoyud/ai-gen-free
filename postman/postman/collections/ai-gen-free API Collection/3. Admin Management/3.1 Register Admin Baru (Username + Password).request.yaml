$kind: http-request
description: Mendaftar akun admin baru menggunakan username dan password (tanpa OTP/verifikasi email).
url: "{{baseUrl}}/admin/register"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "username": "admin123",
      "password": "AdminPassword123!"
    }
order: 1000
