$kind: http-request
description: Mengirim tautan reset ke email terdaftar. Email belum terdaftar → A018. Request ulang < 1 jam → A021. Setelah ganti password < 24 jam → A022. Maks 3 request per IP → A008.
url: "{{baseUrl}}/auth/password-reset"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "email": "user.test123@gmail.com"
    }
order: 11000
